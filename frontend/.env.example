# Copy to .env.local to override.

# Render bundled sample data (no backend needed). Set to "false" for live API.
VITE_USE_MOCKS=true

# Base URL for the live API when VITE_USE_MOCKS=false.
# In dev, "/api" is proxied to http://localhost:8000 (see vite.config.js).
VITE_API_BASE=/api
